# LILO configuration file
# generated by 'liloconfig'
#
# Start LILO global section
# Append any additional kernel parameters:
append=" vt.default_utf8=0"
boot = /dev/hda
compact
lba32
password=""

# Boot BMP Image.
# Bitmap in BMP format: 640x480x8
##  bitmap = /boot/slack.bmp
# Menu colors (foreground, background, shadow, highlighted
# foreground, highlighted background, highlighted shadow):
##  bmp-colors = 255,0,255,0,255,0
# Location of the option table: location x, location y, number of
# columns, lines per column (max 15), "spill" (this is how many
# entries must be in the first column before the next begins to
# be used.  We don't specify it here, as there's just one column.
##  bmp-table = 60,6,1,16
# Timer location x, timer location y, foreground color,
# background color, shadow color.
##  bmp-timer = 65,27,0,255

install = text

# Standard menu.
# Or, you can comment out the bitmap menu above and 
# use a boot message with the standard menu:
#message = /boot/boot_message.txt

# Wait until the timeout to boot (if commented out, boot the
# first entry immediately):
prompt
# Timeout before the first entry boots.
# This is given in tenths of a second, so 600 for every minute:
timeout = 10
# Override dangerous defaults that rewrite the partition table:
change-rules
  reset
# Normal VGA console
vga = 769
# VESA framebuffer console @ 1024x768x64k
# vga=791
# VESA framebuffer console @ 1024x768x32k
# vga=790
# VESA framebuffer console @ 1024x768x256
# vga=773
# VESA framebuffer console @ 800x600x64k
# vga=788
# VESA framebuffer console @ 800x600x32k
# vga=787
# VESA framebuffer console @ 800x600x256
# vga=771
# VESA framebuffer console @ 640x480x64k
# vga=785
# VESA framebuffer console @ 640x480x32k
# vga=784
# VESA framebuffer console @ 640x480x256
# vga=769
# End LILO global section
# Linux bootable partition config begins
image = /boot/vmlinuz-2.6.30.8-slava
  append = "resume=/dev/hda2 irqpoll loglevel=2, console=tty2"
  root = /dev/hda1
  label = 2.6.30.8-slava
  restricted
  read-only

#image = /boot/vmlinuz-2.6.30.7-slava
#  append = "resume=/dev/hda2"
#  root = /dev/hda1
#  label = 2.6.30.7-slava
#  restricted
#  read-only

#image = /boot/vmlinuz-generic-smp-2.6.30.5-smp
#  initrd = /boot/initrd.gz
#  append = "resume=/dev/hda2"
#  root = /dev/hda1
#  label = gen-2.6.30.5
#  restricted
#  read-only

image = /boot/bzImage26296
  append = "resume=/dev/hda2"
  root = /dev/hda1
  label = Linux-26296
  restricted
  read-only

#image = /boot/vmlinuz
#  append = "resume=/dev/hda2"
#  root = /dev/hda1
#  label = Linux
#  restricted
#  read-only
# Linux bootable partition config ends
